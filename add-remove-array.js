// Creating an array of cities
let cities = ["New York", "London", "Tokyo", "Paris"];

// Adding a city to the array
cities.push("Sydney");

// Removing the first city from the array
cities.shift();

// Looping through the array and printing each city
cities.forEach(city => {
    console.log(city);
});
